# Task 1: TypeScript type definition

```typescript
const test = baseTest.extend<{
  transaction: any;
}>({
  // ... implementation will go here
});
```

- Use Playwright's `extend` method with a generic type to define the fixture type
